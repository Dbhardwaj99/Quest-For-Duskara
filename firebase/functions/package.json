{
  "name": "quest-for-duskara-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test --test-concurrency=1 lib/test/*.test.js",
    "test:emulator": "firebase emulators:exec --project quest-for-duskara-test --config ../firebase.json --only auth,firestore,database 'npm test'"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/node": "^22.15.0",
    "firebase": "^12.16.0",
    "typescript": "^5.8.3"
  }
}
